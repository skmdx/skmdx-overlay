--- a/src/pash/cli.py
+++ b/src/pash/cli.py
@@ -69,6 +69,7 @@
     # Set PASH_TOP to point to the src/pash directory
     env = os.environ.copy()
     env["PASH_TOP"] = str(get_pash_top())
+    env["PASH_PYTHON"] = sys.executable
 
     # Pass all arguments to pa.sh
     args = ["bash", str(pa_sh_path)] + sys.argv[1:]
--- a/src/pash/pa.sh
+++ b/src/pash/pa.sh
@@ -33,7 +33,7 @@
 export PASH_BASH_VERSION="${BASH_VERSINFO[@]:0:3}"
 
 ## Create temporary directory and communication FIFOs
-export PASH_TMP_PREFIX="$(mktemp -d /tmp/pash_XXXXXXX)/"
+export PASH_TMP_PREFIX="$(mktemp -d "${TMPDIR:-/tmp}/pash_XXXXXXX")/"
 export PASH_TIMESTAMP="$(date +"%y-%m-%d-%T")"
 export RUNTIME_IN_FIFO="${PASH_TMP_PREFIX}/runtime_in_fifo"
 export RUNTIME_OUT_FIFO="${PASH_TMP_PREFIX}/runtime_out_fifo"
@@ -580,7 +580,7 @@
         echo "python3"
     fi
 }
-export PASH_PYTHON=$(get_pash_python)
+export PASH_PYTHON="${PASH_PYTHON:-$(get_pash_python)}"
 
 pash_setup_server_functions() {
     if [ "${pash_speculative_flag}" -eq 1 ]; then
